# Learned Audit Index

> One line per completed audit. Each entry points to the distilled knowledge that enriched the skill.

## Format

```
- {DATE} | {CHAIN / CONTEST} | {N Mediums + N Highs + ... submitted} | {pointers to enriched files}
```

## Session-start preflight is MANDATORY

See `SKILL.md` § "MANDATORY Session-Start Preflight". Every session must read this file plus `methodology/INDEX.md`, `platform-quirks/{language}.md`, and any project-local DEEP_DIVE_PLAN.md / MANIFEST.md before audit work. Violation = workflow regression.

## Entries

- **2026-04-18** | XRPL Sherlock (C++/rippled) | **5 Mediums** (ESC-1/2/3 + CONF-1 + DEL-1) across **3 of 5 reward pools** (MPT DEX, Confidential MPT, Permission Delegation), remaining 2 pools cleared (Batch + Sponsored Fees) | Added **8 methodology templates** (M-03/04/07/08/09/10/11/12), **5 pattern case studies** in `patterns/xrpl-2026-04/`, full Sherlock contest template bundle in `contest/sherlock/`. XRPL-specific F-01..F-19, R-01..R-34, D-01..D-21, T-01..T-09, L-01..L-17 preserved in project-local manifest at `audit/2026-04-xrp-ledger-april-2026-BadGenius22/scratchpad/learned/00_MANIFEST.md`. Key new templates: **M-08** (5/5 — highest yield), **M-09 SYNC_GAP**, **M-11** (retired-amendment feasibility — saves from submitting PoC-blocked findings), **M-12 granular permission sandbox** (disproportionate pool-diversification value — unlocked 3rd reward pool via DEL-1).
- **2026-04-24** | XRPL Sherlock (C++/rippled) — Domain 19 full 8-agent breadth (time-based expiry cross-amendment attacks) | **0 new Medium+ submissions from Domain 19 directly**; 1 submittable Low candidate **MP-1** (`EscrowCreate.cpp:411` sponsor-reserve over-restriction, XLS-0068 Sponsored Fees, pending user decision — if submitted alongside L-32 would restore reward pool coverage from 2/5 → 3/5) | Added **M-15 Expiry Race Matrix** methodology template (first cross-language time-gated race methodology). Populated ~46 matrix cells across 9 XRPL SLE types (ltESCROW, ltOFFER, ltCHECK, ltNFTOKEN_OFFER, ltPAYCHAN, ltCREDENTIAL, ltDELEGATE, ltPERMISSIONED_DOMAIN, ltESCROW×Sponsored). Project-local manifest additions (XRPL-specific): F-36 (parentCloseTime per-view invariance), F-37 (CanonicalTXSet ordering), F-38 (trifurcated expiry-semantics taxonomy), R-69 (within-ledger time-gate drift REFUTED), R-70 (cancellable-escrow race reduces to ESC-1/2/3), R-71 (RPC read-only immunizes periphery), D-32 (sponsor-refund-before-erase canonical pattern), D-33 (`checkInsufficientReserve` usage discipline), D-34 (Batch `disabledTxTypes` asymmetric-defense surfaces), T-10 (cancellable-escrow exploits require careless issuer), L-33/L-34/L-35, M-13. Key insight validated: **negative-result methodologies still pay** — even with 0 submittable race findings, the M-15 template structure was validated and 3 new framework facts + 3 refuted classes + 3 defensive patterns tighten the next audit's attack surface map.
- **2026-04-24** | XRPL Sherlock (C++/mpt-crypto) — Domain 15 full 8-agent breadth (mpt-crypto systematic audit, 6,364 LoC C cryptographic library for XLS-0096 Confidential MPT) | **0 new Medium+ submissions**; 1 submittable Low candidate **PERIPH-M1** (`bulletproof_aggregated.c` verifier 13-path memory leak, NEW vs kuprum, XLS-0096 already covered by CONF-1 — **HOLD recommended** pending user decision). **Primary hypothesis H4 REFUTED**: ConvertBack Enc(0)-Enc(X) → Enc(-X) soundly defended via two-primitive composition (sigma binds PC_b to plaintext; BP binds verifier-derived pc_rem = PC_b - amount*G). This closes kuprum rippled#6885 + #6886 "POSSIBLE" branch on the SAFE side — biggest severity-ceiling lift opportunity resolved at Medium (unchanged). | Added **M-16 ZK-Proof-Bundle Composition Audit** methodology template (first cross-language ZK-audit methodology). Populated ~50 composition-matrix cells across 4 rippled-reachable + 3 unreachable bundles. Project-local manifest additions (XRPL-specific): F-39 (prover/verifier asymmetric ingress-validation), F-40 (heap-management inconsistency), F-41 (3 orphan public APIs unreachable from rippled), F-42 (74-byte context-hash serializer), F-43 (Send doubly-bound spending balance), F-44 (no-short-circuit AND verification), F-45 (ConvertBack SOUND closes kuprum#6885/6886), R-72..R-81 (10 refuted classes including linkage rho-binding, multi-recipient truncation, BP `x` transitive domain-tag), D-35 (orphan-API detection), D-36 (amount==0 short-circuit), D-37 (verifier-derived remainder), D-38 (two-primitive amount-binding), D-39 (variable-sharing composition), D-40 (no-short-circuit verification), D-41 (fresh-salt nonce defense-in-depth), T-11 (completeness-vs-soundness tradeoff), T-12 (ConvertBack crypto verdict CORRECT), L-36..L-40 (PERIPH-M1 HOLD + 4 informational). **6 cross-language meta-patterns extracted**: verifier-derived remainder, two-primitive amount-binding, orphan-public-API reachability filter, completeness-vs-soundness tradeoff, prover-verifier seckey_verify symmetry seam, paired-transactor binding comparison. **Negative-result validation**: 0 Medium+ submissions but every methodology innovation validated against concrete code + 3-agent convergence on multiple patterns. Library verdict: sound modulo known completeness issues.
- **2026-04-25** | XRPL Sherlock (C++/rippled) — Domain 16 full 8-agent breadth (XLS-0094 Dynamic MPT systematic audit, ~611 LoC `MPTokenIssuanceSet.cpp` + `MPTokenIssuanceCreate.cpp` + flag/SLE consumers) | **0 new Medium+ submissions**; 2 submittable Low candidates: **MP-1** (`MPTokenIssuanceSet` retroactive transfer fee asymmetric lock-in — `EscrowCreate`/`EscrowFinish` snapshot `sfTransferRate` at create but `OfferCreate`/`CheckCreate`/`Payment` live-read; NEW vs kuprum #1721 Confidential bypass) and **P-2** (orphan offers post-CanTrade clear lacking RPC annotation; NEW vs kuprum but CONFLICTED by Vector Scan VS-1 R-25 dedup). **MP-1 HOLD** per T-13 trust-model gating (issuer mutation by-design + no untrusted-actor delegate path per AC §2 sandbox + R-84 sponsor orthogonality + borderline spec-break angle). **P-2 REFUTED-by-R-25** (class already in manifest; RPC-annotation enhancement is not a separate bug; spec-break threshold not met). Reward pool coverage UNCHANGED at 4 Mediums in 2 of 5 pools. | Added **M-17 Mutable-Configuration-Flag Audit** methodology template (first cross-language mutable-config audit methodology). Populated ~70 composition-matrix cells across 12 mutable flags × 8 dimensions on `MPTokenIssuanceSet` (Set→Clear / Clear→Set / mutate-bit immutability / cross-flag coupling / consumer invalidation / preclaim race / batch composition / invariant coverage). Project-local manifest additions (XRPL-specific): F-46 (cross-class preflight firewall), F-47 (fresh consumer reads / no grandfathering), F-48 (XLS-0075 sandbox common-fields admit sponsor), R-82 (6-flag retroactive-clear sweep yields no new SYNC_GAP — CONF-1 is sole instance), R-83 (mutation-allowed-bit immutability structurally enforced), R-84 (sponsor + delegate composition orthogonal to sfAccount comparison), R-85 (kuprum #6603 clear-side variants are same root-cause family), R-86 (holder-side notification on issuer-side flag flip is by-design pre-existing pattern), D-42 (asymmetric on-chain-state-lockin), D-43 (flag-clear cleanup-responsibility), D-44 (mutability-tier polarity audit — D-21 generalization), D-45 (SOLE post-Create writer pattern — L-19 generalization), T-13 (`MPTokenIssuanceSet` mutation IS trusted-issuer action — closes 6 Domain 16 findings), T-14 (per-inner Batch view = canonical XLS-0056 workaround for kuprum #6603 class), L-41 (RPC-annotation gap for orphan offers), L-42 (cross-actor PD-owner trust delegation), L-43 (asymmetric ltOFFER/ltCHECK lock-in gap), L-44 (DomainID lacks `lsmfMPTCanMutate*` opt-in — architectural inconsistency), L-45 (test-coverage gaps). **6 cross-language meta-patterns extracted**: cross-class preflight firewall, asymmetric on-chain-state-lockin, flag-clear cleanup-responsibility, mutability-tier polarity audit, SOLE post-Create writer, fresh consumer reads / no grandfathering. **Negative-result validation**: 0 Medium+ submissions but ~70 cells populated, 2-3-4-way convergence across agents on multiple patterns (mutation-allowed-bit immutability validated by 4 agents independently; asymmetric lock-in validated by 2 agents). Domain verdict: XLS-0094 Dynamic MPT surface is sound modulo (a) CONF-1 already-submitted Medium, (b) kuprum #6603/#6919/#1286/#1298/#1344/#1721 known issues, (c) MP-1/P-2 design-gap candidates HOLD.
- **2026-05-05** | K2 Lending Protocol (Code4rena Stellar Soroban, 2026-04-17 → 2026-05-27, ~15.5k nSLoC, Aave V3 port; 8 passes / 108 hypotheses; agent-verified via /dewaxguard core ×3 invocations) | **1 Medium** (`swap_collateral` missing oracle floor — same consistency-class shape as the QA-Bundle) + **9 Lows** (QA-L01 through QA-L09) + **5 Info** (QA-01 through QA-05) — 14 of 14 QA-Bundle entries follow the consistency-class pattern (documented defensive convention with one or more silent outliers). Yield curve: Pass 1 = 1 Medium; Passes 2-3 = 0; Pass 4 = 2 Lows; Pass 5 = 3 Lows; Pass 6 = 3 Lows + 1 Info; Pass 7+8 = 1 Info. Knee at Pass 6, near-zero yield Pass 7-8 (sub-source-level: V3 patch ancestry, multi-op composition, oracle self-consistency, error handling, casts, unsafe, debug_assert, WASM toolchain — all REFUTED). | Added **M-23 Consistency-Class Sweep** methodology (validated 14/14 — highest yield for Aave V3 forks); **M-24 Fresh-Eyes Surface Sweep** methodology (validated 2/2 novel findings); **rules/realism-filter.md** (first-class permissionless/admin-trust/design-choice/unreachable-precondition tagging — reclassified ~40% of K2 candidate findings; without it the audit would have shipped 4-5 contested admin-trust Mediums); **rules/agent-failure-recovery.md** (codified after Pass 7 manual-fallback produced 3 false negatives that agent retry corrected). Project-local manifest: 9 QA-Lows all consistency-class shape (pause re-check, treasury revert, percent_mul rounding, MAX_USER_RESERVES guard, reward-index flush, close-factor logic, event field, bit-clear method, HF threshold). Key methodology lesson: **manual orchestrator fallback when agents fail produces false negatives** — Pass 7+8 retry with /dewaxguard core agents corrected HF50 (added intra-tx baseline mutability nuance), HF51 (surfaced 3 swallow observations including QA-05), HF44 reclassification (engine `invoke_contract` is SAFE not unsafe).
- **2026-05-06** | K2 Lending Protocol (Code4rena Stellar Soroban) — exploratory continuation: 1 additional Medium (`prepared-liquidation grief via repay-on-behalf` — DD-1, multi-step exploit chain bypassing V12). Final K2 submission state: 2 Mediums (DD-1 + DD-4 oracle floor) + 8 Lows + 7 Non-Critical. **Two false-positive candidates investigated and reverted as V12 duplicates** (DD-5 `update_atoken` caller forwarding → V12 #44797; L-09 TTL-expiry default-on-read → V12 #44792 with `### Invalid Reason` documenting Soroban v23 archive-restore). ~1.5h wasted total before discovery → triggered **M-25 V12-style AI-auditor finding-index pre-grep** methodology + companion `scripts/grep_v12.sh` helper. Validation: re-running the same dedup queries against the K2 V12 corpus (109 findings, 125k lines) returns the dup entries in <1s. Permanent platform-knowledge corpus surfaced: 66 V12 Invalid-marked entries documenting Soroban-v23 / public-entry-point / admin-trust / oracle-dependency landmines. Key methodology lesson: **structured AI-auditor outputs (V12-style) need a different dedup workflow than human-curated catalogs (M-13/Kuprum-style)** — V12's consistent field structure enables programmatic pre-grep that human-curated catalogs don't.
- **2026-04-25** | XRPL Sherlock (C++/rippled) — Domain 18 full 8-agent breadth (AccountDelete × new-SLE matrix systematic audit, 442 LoC `AccountDelete.cpp` + 9 cleanup helpers + new-SLE writers `SponsorshipSet.cpp`/`DelegateSet.cpp`/`LoanSet.cpp`/`MPTokenAuthorize.cpp`/`EscrowCreate.cpp`) | **0 new Medium+ submissions**; **1 contested PARTIAL CONFIRMED Low post-arbitration**: **ECS18-1** (confidential MPT holder soft-lockup at `MPTokenAuthorize.cpp:78-95` NEW block in `ac4f142` — Vector Scan VS18-3 incorrectly REFUTED citing only L44-48; merge orchestrator arbitrated via direct source-code diff and overrode VS18-3, confirming ECS18-1 stands; HOLD per T-13 trust-model gating + by-design defensive code per L88-89 comment + reward pool XLS-0096 already covered by CONF-1). 1 RPC-only HOLD candidate: **F-PER-1** (`account_objects` deletion_blockers omits ltLOAN_BROKER/ltLOAN/ltAMM — CONTEST_FAQ empty-feature-label invalid risk). Reward pool coverage UNCHANGED at 4 Mediums in 2 of 5 pools. 4 known-issue dedups (kuprum #6889/#6892/#6893/#6900) all surface as predicted by the M-18 matrix — confirms negative-result methodology multiplier. | Added **M-18 Account-Lifecycle Cleanup-Switch Completeness Matrix** methodology template (first cross-language account-lifecycle audit methodology). Populated ~28 matrix cells × 8 dimensions on AccountDelete × new-SLE × held-state × dormancy-stage. Project-local manifest additions (XRPL-specific): F-49 (block-by-default switch), F-50 (sole post-Create writer for sfSponsoringAccountCount), F-51 (3-layer sanity guard chain), F-52 (signer-bound dormancy timer), F-53 (NEW confidential MPT block at MPTokenAuthorize.cpp:78-95 — codifies arbitration verdict), R-87 (REJECTED-POST-ARBITRATION — VS18-3's incorrect REFUTAL slot reserved as rejected), R-88 (ltLOAN borrower-must-sign), R-89 (H18-4 timing-attack class refuted as known-issue composition), R-90 (sponsor refund decrement bypass refuted), R-91 (legacy ltDELEGATE orphan OOS), D-46 (block-by-default + informative TER pattern), D-47 (tier-A/B/C/D adversarial-creation × cleanup-coverage classification), D-48 (cascade-aware reserve refund), D-49 (sole post-Create writer for per-ACCOUNT counts — D-45 generalization), D-50 (confidential-residue F-16 family unifying CONF-1 + kuprum #6869 + kuprum #6887 + ECS18-1), T-15 (block-by-default UX-vs-safety trade-off), L-46 (CLOSED — VS18-2 ltLOAN resolved by R-88), L-47 (F-16 architectural fix would close 4-finding family), L-48 (no invariant catches tier-D regressions), L-49 (ltDELEGATE silent-revoke UX gap). **8 cross-language meta-patterns extracted**: block-by-default + informative TER (D-46), tier-A/B/C/D classification (D-47), cascade-aware reserve refund (D-48), sole post-Create writer (D-49), confidential-residue family (D-50), multi-AccountID-keyed SLE walk gap (F18-INV-1 / L-48), dormancy-as-aggravator class (composition with cleanup-blocker = permanent lockup), **cross-agent contradiction handling protocol** (codified into M-18 Phase 3 — when agents disagree on same surface, mandatory source-code arbitration with both versions side-by-side). **Methodology lesson** (highest-value contribution from D18): VS18-3 vs ECS18-1 contradiction surfaced citation-without-end-to-end-read failure mode; arbitration overrode VS18-3 → ECS18-1 wins → F-53 codifies actual behavior. Domain verdict: AccountDelete × new-SLE matrix is architecturally sound modulo (a) ECS18-1 contested Low HOLD, (b) kuprum #6889/#6892/#6893 known issues, (c) F-PER-1 RPC-only HOLD. Sole tier-D anti-pattern in delta = ltSPONSORSHIP (kuprum #6892).

- **2026-06-04** | XRPL Sherlock 1260 (C++/rippled) — **post-audit improvement protocol** (retrospective vs final preliminary-reward gist; not a new audit) | Audit outcome: **5 valid** (F1, F46, F88, F9, F72 ≈ $3,047, 3 of 5 reward pools) + 4 known/informational (K2 escrow ×3, D38 MergeInbox). **0/9 High-or-Critical recall** — the gap analyzed here. RC distribution of the 9 H/C misses: **3× RC-METHOD** (F4/F3 signature-replay; F30 ZK-forgery), **2× RC-DEPTH** (F13 teardown; F16 fee-round-to-zero), **4× RC-AGENT** (F60 $24k-solo BookStep fee-account; F15 AMMClawback auth; F39 book_offers; F82 sponsored cross-currency — analyzed-but-mis-reasoned, no rule change per RC-AGENT presumption gate). | Added **M-30** (signature-binding/replay/domain-separation — new class, 0 prior coverage); extended **M-16** (Phase 2.7 adversarial forgery construction — composition-correct ≠ forgery-resistant; M-16 had concluded "sound" yet missed F30 drain-shared-backing Critical), **M-18** (broadened to all object-teardown ops + shared-object grief variant — was AccountDelete-only, missed F13), **M-19** (E11 round-to-zero fee value-escape — missed F16). **Meta-root-cause: RC-CONTEXT domain mismatch** — dewaxguard's vector libraries are smart-contract-oriented; rippled is a C++ consensus node + ZK crypto, so the signature-replay / ZK-soundness / DEX-fee-attribution attack *classes* were absent, not mis-reasoned. v1.18.0.

## Growth protocol

After every audit:
1. **Promote generalizable methodology** to `methodology/` — anything M-xx that applies cross-language.
2. **Archive per-finding case studies** to `patterns/{chain}-{date}/` — one file per submitted finding.
3. **Enrich platform-quirks** for the audited language.
4. **Add contest-specific templates** to `contest/{platform}/` if the audit revealed new rules.
5. **Append one line here** summarizing what was added.
6. **Commit + push** the skill repo to `BadGenius22/dewaxguard` GitHub for cross-machine portability.

## Methodology maturity tracking

Each methodology template in `methodology/` should list its validated findings in its "Validated findings" section. Re-validation across multiple audits strengthens confidence.

As of 2026-05-06:
- **M-25** v12-style-ai-auditor-dedup: 1 structural validation (K2 Code4rena exploratory continuation; 2/2 false-positive candidates would have been killed in <1s via `grep_v12.sh` had it existed at audit start: DD-5 `update_atoken` matched V12 #44797 by title, L-09 TTL-expiry matched 5 V12 Invalid entries via `--invalid-only` mode). **Time savings**: ~1.5h wasted investigation + ~0.5h documentation revert per audit when V12-style index ships. **Permanent platform-knowledge corpus**: 66 Invalid-marked entries become pre-audit reading material. Distinct from M-13 (Kuprum-style human-curated catalogs); M-25 is for STRUCTURED AI-auditor outputs with consistent fields. Companion: `scripts/grep_v12.sh`.

As of 2026-05-05:
- **M-23** consistency-class-sweep: 1 structural validation (K2 Code4rena 2026-04 → 2026-05; 14/14 QA-Bundle entries follow this pattern across 8 audit passes; ~89% of all findings; 1 Medium also fits the shape). **Highest-yield methodology for Aave V3 forks** — supersedes M-08 for fork-style protocols. Applicable to any protocol with ≥3 instances of the same defensive macro/check.
- **M-24** fresh-eyes-surfaces: 1 structural validation (K2 Code4rena; 2/2 novel findings — FE-1 in Pass 5 and Observation A in Pass 6, both surfaces no themed pass covered). **Highest-yield late-stage methodology** — apply once per audit after ≥1 prior pass.

As of 2026-04-24:
- **M-03** inverse-trust: 5 validations (XRPL ESC-1/2/3 + CONF-1 + DEL-1)
- **M-04** feature-pool-coverage: 2 validations (XRPL ESC-1, ESC-3)
- **M-07** post-finding-sweep: 1 validation (XRPL L-14 → ESC-3)
- **M-08** holder-plants-trap: **5 of 5 Mediums — highest-yield template**
- **M-09** SYNC_GAP: 1 validation (XRPL CONF-1)
- **M-10** prior-audit-dedup: 1 validation (XRPL submission prep)
- **M-11** retired-amendment-feasibility: 1 save (XRPL SPON-M1 correctly NOT submitted)
- **M-12** granular-sandbox: 1 validation (XRPL DEL-1) — first Permission Delegation pool hit
- **M-15** Expiry Race Matrix: 1 structural validation (XRPL Domain 19; 46 cells populated, 0 Medium+ findings — template validated, adjacent Low MP-1 surfaced). Negative-result methodologies still tighten attack surface maps.
- **M-16** ZK-Proof-Bundle Composition: 1 structural validation (XRPL Domain 15; 50 cells populated across 4 reachable + 3 unreachable bundles; 0 Medium+ findings; PERIPH-M1 Low candidate HOLD). **6 cross-language meta-patterns** validated: verifier-derived remainder (D-37), two-primitive amount-binding (D-38), orphan-public-API reachability (D-35), completeness-vs-soundness tradeoff (T-11), prover-verifier seckey_verify seam (F-39), paired-transactor comparison (F-42). Primary attack class (Enc(0)-Enc(X) extraction — kuprum rippled#6885/#6886) REFUTED — library is sound modulo known completeness issues.
- **M-17** Mutable-Configuration-Flag Audit: 1 structural validation (XRPL Domain 16; ~70 cells populated across 12 mutable flags × 8 dimensions; 0 Medium+ findings; 2 PARTIAL Low candidates MP-1 HOLD per T-13 trust-model + P-2 REFUTED-by-R-25). **6 cross-language meta-patterns** validated: cross-class preflight firewall (F-46/D-42), asymmetric on-chain-state-lockin (D-42), flag-clear cleanup-responsibility (D-43), mutability-tier polarity audit (D-44 generalization of D-21), SOLE post-Create writer (D-45 generalization of L-19), fresh consumer reads / no grandfathering (F-47). Mutation-allowed-bit immutability (`lsmfMPT*CanMutate*` — H3 hypothesis) confirmed structurally enforced by 4-agent independent convergence (R-83). Domain verdict: XLS-0094 Dynamic MPT surface sound modulo CONF-1 (already submitted) + kuprum known issues.
- **M-18** Account-Lifecycle Cleanup-Switch Completeness Matrix: 1 structural validation (XRPL Domain 18; 442 LoC `AccountDelete.cpp` + 9 cleanup helpers + new-SLE writers; ~28 cells populated across new-SLE × held-state × dormancy-stage × cleanup-coverage with 8 dimensions; 0 Medium+ findings; 1 contested PARTIAL CONFIRMED Low post-arbitration ECS18-1; 4 known-issue dedups (kuprum #6889/#6892/#6893/#6900) all surface as predicted by matrix). **8 cross-language meta-patterns** validated: block-by-default + informative TER (D-46), tier-A/B/C/D adversarial-creation × cleanup-coverage classification (D-47), cascade-aware reserve refund (D-48), sole post-Create writer for per-ACCOUNT counts (D-49 generalization of D-45), confidential-residue F-16 family unifying CONF-1 + kuprum #6869 + kuprum #6887 + ECS18-1 (D-50), multi-AccountID-keyed SLE walk gap (L-48), dormancy-as-aggravator class (composition with cleanup-blocker = permanent lockup), **cross-agent contradiction handling protocol** (Phase 3 sub-methodology — when agents disagree on same surface, mandatory source-code arbitration with both versions side-by-side; codified after VS18-3 vs ECS18-1 arbitration). Tier-D anti-pattern (unilateral creation + block-by-default no cascade) = sole instance ltSPONSORSHIP (kuprum #6892). 5-way agent concurrence on F-49 block-by-default switch. Domain verdict: AccountDelete × new-SLE matrix is architecturally sound modulo ECS18-1 contested Low HOLD + kuprum known issues + F-PER-1 RPC-only HOLD.

## High-yield summary (for first-pass audits)

Start with M-08 on every privileged operation. Follow with M-09 on every aggregate counter. If the contest has per-feature reward pools, run M-12 on granular permissions. Pre-submission: M-04 + M-10 + M-11.

## Cross-language applicability

All 8 methodology templates are chain-agnostic in the abstract pattern; only the syntactic triggers change. Primary mappings:

| Template | EVM | Solana | Move | Soroban | XRPL |
|----------|-----|--------|------|---------|------|
| M-03 inverse-trust | compliance roles | authority-gated ops | capability issuers | admin/authority | issuer compliance |
| M-08 holder-plants-trap | wrapper contracts | PDA escrows | coin wrappers | claimable balances | uncancellable escrows |
| M-09 SYNC_GAP | totalSupply vs balanceOf | mint.supply vs ATAs | aggregator vs resources | TTL counters | MPT counters vs fields |
| M-11 retired-feasibility | post-hardfork | retired instructions | deprecated modules | — | retired amendments |
| M-12 granular-sandbox | AccessControl + hooks | Anchor access_control | Capability types | fine-grained auth | checkGranularSemantics |
| M-15 expiry-race-matrix | block.timestamp + multicall + MEV-boost | Clock::slot + instruction array + Jito | Move time + PTB (Sui) | ledger.timestamp + TTL counters | parentCloseTime + Batch + CanonicalTXSet |
| M-16 zk-proof-bundle-composition | Aztec/Railway/zkBob confidential; Groth16/Plonk verifiers | light-protocol, Elusiv bundles | Native ZK verifiers (future Sui/Aptos) | Aleo record programs; Noir/Circom range proofs | mpt-crypto (this audit); libsecp256k1-based sigma/BP bundles |
| M-17 mutable-config-flag-audit | TransparentProxy/UUPS/Diamond + mutable params | governance + token-2022 extensions | mutable objects with `key + store` (Sui); resource accounts (Aptos) | TTL counters + admin-mutable rates | MPTokenIssuanceSet + sfMutableFlags |
| M-18 account-lifecycle-cleanup-switch | `selfdestruct` (deprecated EIP-6049) + ERC-6900 plugin uninstall | `close` instruction with lamport recovery + reentrancy | `object::delete` with `key + store` ability cascade (Sui); `move_from` resource destruction (Aptos) | `keeper.Delete()` patterns + staking-delegation cleanup | AccountDelete + nonObligationDeleter switch + 256-ledger dormancy |
